# RARE26 技術筆記

## 2026-08-28　測試集幾何：主辦方是「壓扁」，不是「裁切」

**問題**：訓練影像約 640×512（實測 587–655 × 471–512），
官方樣本 `example_batch_0_15.tiff` 是 **(16, 512, 512, 3) uint8**，正方形且無黑邊。
兩者長寬比不同 → 必須知道主辦方怎麼把它變成正方，否則預處理的幾何對不上。

**方法**：水平／垂直梯度能量比（影像被水平壓縮 → 水平梯度變陡 → 比值上升）。

| 影像 | 梯度異向性（中位數） |
|---|---|
| 官方測試堆疊 512×512 | **1.848** |
| 訓練圖壓扁成 512×512 | **1.637** |
| 訓練圖中心裁切成 512×512 | 1.272 |
| 訓練原圖 ~640×512 | 1.339 |

裁切幾乎不改變異向性（1.272 ≈ 1.339，合理，因為裁切不重採樣）。
測試集的 1.848 明顯落在壓扁那一側。

**結論**：主辦方對測試影像做的是**非等比縮放（壓扁）**。

**後果**：預處理必須用 `Resize((H, W))` 直接壓扁，
**不可**用 timm 預設的 `Resize(短邊) + CenterCrop`——後者會切掉兩側，
產生與測試時不同的幾何。官方 baseline 的 `transforms.Resize((224, 224))` 是對的。

**未解**：測試集 1.848 > 壓扁後的 1.637。可能是測試集原圖比訓練集更寬
（12 家中心、含前瞻收案，器材設定不同），或另有降採樣。方向已定，量級未定。

## 資料組成

| | ndbe（正常） | neo（病灶） |
|---|---|---|
| center_1 | 2,218 | 61 |
| center_2 | 719 | 97 |
| 合計 | 2,937 | 158 |

兩個中心的盛行率差很多（2.7% vs 11.9%），且驗證／測試集來自
BONSAI 的 12 家中心。所以本機驗證以 **LOCO（leave-one-center-out）** 為主，
分層 CV5 只拿來確認訓練訊號存在。
